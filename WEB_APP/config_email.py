# Email Configuration
# Update these settings with your email provider credentials

EMAIL_CONFIG = {
    'sender_email': 'smhanifa3@gmail.com',  # Your email address
    'sender_password': 'sgvm dmdu renq lzoz',  # App password or email password
    'smtp_server': 'smtp.gmail.com',         # SMTP server
    'smtp_port': 587,                         # SMTP port (TLS)
    'use_tls': True,                          # Use TLS encryption
    'enabled': True                           # Set to True to enable email notifications
}

# Available email providers:
# Gmail: smtp.gmail.com (port 587 with TLS or 465 with SSL)
# Outlook: smtp.live.com (port 587 with TLS)
# Yahoo: smtp.mail.yahoo.com (port 587 or 465)
# Office365: smtp.office365.com (port 587 with TLS)
